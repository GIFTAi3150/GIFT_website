# 実績掲載ページ（/works）— 要確認ファクト一覧

`/works` はソフトローンチ（Header/Footer/sitemap 未配線）で先行実装した。実在クライアントの
掲載可否・ロゴ・社名は未確定で、コピー内はすべて仮値。仮値は
`src/app/works/_components/worksContent.ts` 内に `// 【要確認】` コメントを付けてある。
本ドキュメントと 1:1 対応しているので、値が確定したら両方（コピーとこの表）を更新すること。

| # | 項目 | 現在の仮値 | worksContent.ts のキー | 確認先メモ |
|---|------|-----------|--------------------------|-----------|
| 1 | 掲載企業リスト（社数） | 実企業2社（株式会社Sales Index / 株式会社RAYS、表記確定済み）+ 仮6枠 | `LOGO_WALL.tiles`（配列。要素数=掲載社数） | 掲載を承諾いただいた企業を確定し、社数ぶん配列を差し替える。配列駆動のため増減は要素の追加・削除のみで対応可能 |
| 2 | 各社ロゴデータ | 未回収（LOGO文字のプレースホルダ表示） | `LOGO_WALL.tiles[].logoSrc`（未指定時はプレースホルダにフォールバック） | 形式は **SVG/PNG（透過背景）推奨**。回収後 `public/img/works/` に配置し、該当タイルの `logoSrc` にパスを設定 |
| 3 | 正式社名 | 「株式会社サンプルA」〜「株式会社サンプルH」（仮名） | `LOGO_WALL.tiles[].companyName` / `PICK_UP.cases[].companyName` | 各社の正式表記（法人格の位置・株式会社/合同会社等の別・英語表記要否）を確認 |
| 4 | 業種表記 | 「通信 ［仮・業種］」「小売 ［仮・業種］」（PickUp 2社のみ設定、LogoWall側は業種表記なし） | `PICK_UP.cases[].industry` | 業種の表記ゆれ（自社の呼称 vs 一般的な業種分類）を各社に確認。LogoWallタイルにも業種を出すかは要判断 |
| 5 | 取組内容文言 | 「［仮テキスト］」を含む2文（コールセンター立ち上げ／店舗DX） | `PICK_UP.cases[].body` | 各社に開示可能な範囲（守秘義務・NDAの制約）を確認のうえ、実際の取組内容を1〜2文で確定 |
| 6 | 掲載順ルール | **確定: 五十音順**（2026-09-02、読み仮名も確認済み: セールスインデックス/レイズ） | `LOGO_WALL.tiles`（配列順＝表示順） | 五十音順／掲載承諾日順／取引額順など、依頼者の希望する並び順ルールを確認し配列順を合わせる |
| 7 | 導入事例の業種・内容 | 仮2件（通信/小売、文言も仮） | `PICK_UP.cases`（industry + body。**会社名は非表示方針で確定** 2026-09-02） | 実事例の業種表記と取組内容の文言を確定。件数を増やすかも含め要判断 |

## 反映手順

1. この表の「現在の仮値」列を確定値に更新する。
2. `worksContent.ts` の対応キーを書き換え、該当行の `// 【要確認】` コメントを削除する。
3. ロゴ画像を追加する場合は `public/img/works/` に配置し、`logoSrc` を設定する（プレースホルダ表示から自動的に切り替わる）。
4. すべての行のコメントが消え、実ロゴ・実社名に差し替わったら、このファイルは削除するかアーカイブしてよい。
